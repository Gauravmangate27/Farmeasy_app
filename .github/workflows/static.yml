# .github/workflows/deploy.yml
name: Deploy index.html from templates folder to Pages

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact (only index.html)
        uses: actions/upload-pages-artifact@v3
        with:
          path: '# .github/workflows/deploy.yml
name: Deploy templates folder to GitHub Pages

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact (templates folder)
        uses: actions/upload-pages-artifact@v3
        with:
          path: './templates'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
